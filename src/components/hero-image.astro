---
import { Picture } from "@astrojs/image/components";
import heroImage from "~/assets/caught-in-joy-unsplash.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Picture
  class="h-full w-full object-cover"
  src={heroImage}
  widths={widths}
  sizes={sizes}
  alt="Recording studio image"
/>
